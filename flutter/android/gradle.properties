org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental compilation is disabled because the pub cache (C:\) and the
# project (D:\) are on different filesystem roots. Kotlin's incremental cache
# tries to reconcile those paths and fails with "this and base files have
# different roots" (e.g. on the speech_to_text plugin). Disabling incremental
# compilation bypasses that cache entirely.
kotlin.incremental=false
