<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Location permissions ────────────────────────────────────────────
         Milestone 2: Foreground location (fine + coarse).
         ACCESS_FINE_LOCATION   — full GPS accuracy.
         ACCESS_COARSE_LOCATION — fallback network/WiFi location.

         Milestone 5: Background location + foreground service.
         ACCESS_BACKGROUND_LOCATION — required on Android 10+ (API 29+) for
                                      location access when the app is not in
                                      the foreground.  The user sees a
                                      separate "Allow all the time" prompt.
         FOREGROUND_SERVICE         — required to declare a foreground service.
         FOREGROUND_SERVICE_LOCATION — required on Android 14+ (API 34+) for
                                       foreground services that access location.
         POST_NOTIFICATIONS         — required on Android 13+ (API 33+) to
                                      show the foreground service notification.
         WAKE_LOCK                  — keeps the CPU awake during background
                                      location updates so fixes are not dropped.
    ──────────────────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- INTERNET: required to reach the future hosted Google ADK conversation
         agent API.  Kept so the app retains network access after the old
         FastAPI/ORCA integration is removed. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- RECORD_AUDIO: required for the search-bar voice input (speech_to_text).
         Requested at runtime; handled gracefully by SpeechService. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Bluetooth: some devices route mic audio via a headset; optional but
         avoids silent failures on BT-only mic setups. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:label="samudra_ai"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:usesCleartextTraffic="true">
        <!-- android:usesCleartextTraffic enabled so the physical device can
             reach the dev backend over plain HTTP (LAN IP:8000). The backend
             runs locally as http://... and is NOT TLS in development. Remove
             or replace with a networkSecurityConfig before production HTTPS. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required on Android 11+ for speech_to_text to resolve the system
             speech recognition service. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
